# TypeScript Claw Server Archive

This is a frozen snapshot of the retired TypeScript BrowserClaw server. The
active implementation lives in `packages/browseros-agent/apps/claw-server-rust`.

The snapshot is reference-only. It is intentionally outside the
`packages/browseros-agent` workspace, and active builds, tests, development
tools, releases, and runtime code must not depend on it.
